import React from "react";
import styles from "./Header.module.css"

const Header = ()=>{
    return(
        <h1 className="header" className={styles.header}>
        Currency Convertor</h1>
    );
}


export default Header;